# Deploy Backend

App Runner is no longer used. Backend deployment is now supported on:

- **EC2 (recommended)** → see `DEPLOY_BACKEND_EC2.md`
- **EKS (advanced)** → see `DEPLOY_BACKEND_EKS.md`

If you want App Runner again later, I can re-enable it.
